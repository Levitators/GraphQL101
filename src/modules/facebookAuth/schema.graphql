type User {
  id: Int!
  email: String!
  googleUserId: String!
  facebookUserId: String!
  username: String!
}

type AuthResponse {
  auth_token: String!
  user: User
}

type Mutation {
  AuthenticateFacebookUser(facebookToken: String!): AuthResponse
}

